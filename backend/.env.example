# Copy this file to `.env` in the same folder and paste your real key there.
# .env is git-ignored — never commit your actual key.

GEMINI_API_KEY=your_gemini_api_key_here

# Origins allowed to call this API (comma-separated). The Vite dev server
# default is already included.
CORS_ORIGINS=http://localhost:5173
